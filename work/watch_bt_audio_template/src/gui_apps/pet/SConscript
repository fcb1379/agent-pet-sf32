from building import *
import os

cwd = GetCurrentDir()
src = Glob('*.c')
app_utils = os.path.join(cwd, '..', '..', 'app_utils')

group = DefineGroup('App_watch_demo', src, depend = [''], CPPPATH = [cwd, app_utils])

Return('group')
